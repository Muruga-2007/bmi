Generate Tailwind design tokens for a cinematic AI website (A-Zentrix):
- Colors: background (dark), surface (glass), primary glow (choose accent), muted text, border, success, warning.
- Gradients: hero, footer horizon, CTA shimmer.
- Shadows: soft glow presets (small, medium, large).
- Radii: subtle rounding scale.
- Spacing & typography scale (desktop & mobile).
Return as a clean Tailwind config extension (module.exports = { theme: { extend: { ... } } }).
